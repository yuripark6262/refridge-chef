# PRD — 2단계: 레시피 생성

## 1. 개요
1단계에서 확정된 **재료 목록**과 사용자 선호(선택 사항)를 입력받아, `google/gemma-4-26b-a4b-it:free` 모델로 실현 가능한 레시피를 1~3개 생성해 보여준다.

## 2. 목표 / 비목표
- **목표**
  - 재료 목록 기반으로 만들 수 있는 레시피를 추천한다.
  - 레시피를 구조화(제목, 재료, 조리 단계, 조리 시간, 난이도)하여 표시한다.
  - 보유 재료 대비 부족한 재료를 구분해 표시한다.
- **비목표**
  - 레시피 영구 저장/즐겨찾기 (→ 3단계)
  - 실시간 영양 성분 DB 연동 (모델 추정치만 사용)

## 3. 사용자 스토리
- 사용자로서, 지금 있는 재료로 만들 수 있는 요리를 추천받고 싶다.
- 사용자로서, 조리 순서와 예상 시간을 단계별로 보고 싶다.
- 사용자로서, 채식·알레르기·요리 시간 같은 조건을 반영받고 싶다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F2-1 | 1단계의 재료 목록을 입력으로 수신 |
| F2-2 | 선택적 필터: 식단(채식 등), 알레르기, 최대 조리 시간, 인분 수 |
| F2-3 | OpenRouter API로 레시피 생성 요청 (구조화 JSON 출력) |
| F2-4 | 레시피 카드 렌더링: 제목 / 필요 재료(보유·부족 구분) / 단계 / 시간 / 난이도 |
| F2-5 | "다른 레시피 보기"로 재생성 |
| F2-6 | 각 레시피를 3단계(저장)로 넘길 수 있는 데이터 형태로 유지 |

## 5. 기술 설계
- **모델**: `google/gemma-4-26b-a4b-it:free`
- **엔드포인트**: 1단계와 동일한 chat completions (텍스트 전용 호출)

### 제안 API
```
POST /api/recipes
  body: {
    ingredients: ["달걀", "대파", "밥"],
    preferences: { diet: "none", allergies: [], maxMinutes: 30, servings: 2 }
  }
  resp: { recipes: [ { title, ingredients:[{name, have:bool}], steps:[...], minutes, difficulty } ] }
```

### 모델 프롬프트 (예시)
```
너는 요리 추천 도우미다. 아래 보유 재료로 만들 수 있는 레시피를 최대 3개 제안하라.
가능하면 보유 재료를 최대한 활용하고, 꼭 필요한 부족 재료는 have=false로 표시하라.
아래 JSON 형식으로만 답하라.
{"recipes":[{"title":"","ingredients":[{"name":"","have":true}],"steps":[""],"minutes":0,"difficulty":"쉬움|보통|어려움"}]}

보유 재료: {ingredients}
조건: {preferences}
```

### 안정성
- JSON 스키마 검증 후 실패 시 1회 재시도.
- 부족 재료가 과도하면(예: 보유 재료를 거의 안 씀) 제외/재요청.

## 6. UI/UX
- 2단계 진입 시 재료 확인 + 선호 조건 입력 폼
- "레시피 추천" 클릭 → 로딩 → 레시피 카드 목록
- 각 카드: 펼치기로 상세 단계 표시, "저장" 버튼(→ 3단계), "다른 레시피" 버튼

## 7. 엣지 케이스
- 재료가 너무 적음(1~2개) → 기본 양념 가정 안내와 함께 간단 레시피 제안
- 상충 조건(예: 채식 + 유일 재료가 고기) → "조건에 맞는 레시피가 없어요" 안내
- 모델이 비현실적 레시피 생성 → 재생성 유도

## 8. 완료 기준 (Acceptance Criteria)
- [ ] 재료 목록으로 1개 이상의 구조화된 레시피가 표시된다.
- [ ] 보유/부족 재료가 시각적으로 구분된다.
- [ ] 선호 필터가 결과에 반영된다.
- [ ] 레시피가 3단계에서 저장 가능한 형태로 존재한다.

## 9. 의존성 / 참고
- 이전 단계: [PRD_step1.md](PRD_step1.md)
- 다음 단계: [PRD_step3.md](PRD_step3.md)
