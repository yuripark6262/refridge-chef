# PRD — 1단계: 냉장고 이미지 입력 & 재료 인식

## 1. 개요
냉장고 사진을 업로드하면 `google/gemma-4-26b-a4b-it:free` 모델(OpenRouter 경유)이 사진 속 식재료를 인식하여 목록으로 반환하는 기능을 구현한다. 이후 2단계(레시피 생성)의 입력이 되는 **재료 목록**을 안정적으로 만들어 내는 것이 목표다.

## 2. 목표 / 비목표
- **목표**
  - 사용자가 이미지를 업로드/촬영해 서버로 전송할 수 있다.
  - 모델이 이미지에서 식재료를 인식하고 구조화된 목록(JSON)으로 반환한다.
  - 인식 결과를 사용자가 화면에서 확인하고 수정(추가/삭제)할 수 있다.
- **비목표**
  - 레시피 생성 (→ 2단계)
  - 사용자 계정/저장 (→ 3단계)
  - 수량·유통기한 정밀 추정 (인식되면 표시하되 정확도 보장 안 함)

## 3. 사용자 스토리
- 사용자로서, 냉장고 사진을 올리면 어떤 재료가 있는지 자동으로 목록을 받고 싶다.
- 사용자로서, 잘못 인식된 재료를 지우거나 빠진 재료를 직접 추가하고 싶다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F1-1 | 이미지 업로드 UI (드래그앤드롭 + 파일 선택 + 모바일 카메라 촬영 지원) |
| F1-2 | 클라이언트에서 이미지 리사이즈/압축 후 base64 data URL로 인코딩 |
| F1-3 | 백엔드가 OpenRouter chat completions API에 이미지 + 프롬프트 전송 |
| F1-4 | 모델 응답을 재료 배열(JSON)로 파싱 |
| F1-5 | 인식된 재료를 태그(칩) 형태로 표시, 개별 삭제 및 수동 추가 가능 |
| F1-6 | 확정된 재료 목록을 2단계로 전달 (세션 상태 또는 API 반환값) |

## 5. 기술 설계
- **모델**: `google/gemma-4-26b-a4b-it:free`
- **엔드포인트**: `POST https://openrouter.ai/api/v1/chat/completions`
- **인증**: `Authorization: Bearer <openrouter_api_key>` (`.env`에서 로드, 클라이언트 노출 금지)
- **이미지 전송 형식**: `image_url` 콘텐츠 파트에 `data:image/png;base64,...` data URL
- **프롬프트 전략**: JSON만 출력하도록 지시하여 파싱 안정화

### 제안 API
```
POST /api/recognize
  body: { image: "data:image/...;base64,..." }
  resp: { ingredients: [{ name: "달걀", confidence: 0.9 }, ...] }
```

### 모델 프롬프트 (예시)
```
너는 냉장고 사진에서 식재료를 인식하는 도우미다.
사진에 보이는 식재료만 골라 아래 JSON 형식으로만 답하라. 설명 문장 금지.
{"ingredients": [{"name": "재료명(한국어)", "confidence": 0.0~1.0}]}
```

### 응답 파싱 주의
- 모델이 코드펜스(```json```)로 감쌀 수 있으므로 펜스 제거 후 파싱.
- 파싱 실패 시 1회 재시도 또는 사용자에게 "인식 실패, 수동 입력" 유도.

## 6. UI/UX
1. 초기 화면: 업로드 영역 + "재료 인식하기" 버튼
2. 처리 중: 로딩 인디케이터
3. 결과 화면: 업로드한 이미지 썸네일 + 재료 칩 목록 + 편집 컨트롤 + "레시피 추천 받기" 버튼(→ 2단계)

## 7. 엣지 케이스
- 음식이 아닌 사진 → 빈 목록 + "재료를 찾지 못했어요" 안내
- 대용량 이미지 → 업로드 전 클라이언트 리사이즈(예: 긴 변 1024px)
- API 오류/타임아웃 → 재시도 버튼 + 사용자 친화적 에러 메시지
- 무료 모델 rate limit → 재시도 백오프 및 안내

## 8. 완료 기준 (Acceptance Criteria)
- [ ] 이미지를 업로드하면 5~15초 내에 재료 목록이 표시된다.
- [ ] 재료를 삭제/추가할 수 있다.
- [ ] 확정된 목록이 2단계로 전달 가능한 형태로 존재한다.
- [ ] API 키가 프론트엔드 번들/네트워크 응답에 노출되지 않는다.

## 9. 의존성 / 참고
- 검증된 API 호출 방식: 저장소의 `test_api.py`의 이미지 인식 로직 참고.
- 다음 단계: [PRD_step2.md](PRD_step2.md)
