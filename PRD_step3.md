# PRD — 3단계: 사용자 프로필 & 레시피 저장

## 1. 개요
사용자 프로필을 만들어 2단계에서 생성한 레시피를 저장하고, 다시 방문했을 때 저장한 레시피와 개인 선호를 불러올 수 있게 한다. 앱을 1회성 도구에서 지속 사용 서비스로 확장하는 단계다.

## 2. 목표 / 비목표
- **목표**
  - 사용자 계정(프로필) 생성 및 로그인.
  - 생성한 레시피를 프로필에 저장/조회/삭제.
  - 개인 선호(식단, 알레르기, 기본 인분 수)를 저장해 2단계에 자동 반영.
- **비목표**
  - 소셜 공유/팔로우, 댓글 (후속 단계로 보류)
  - 결제/구독

## 3. 사용자 스토리
- 사용자로서, 마음에 드는 레시피를 저장해 두고 나중에 다시 보고 싶다.
- 사용자로서, 내 알레르기·식단 정보를 한 번 저장하면 매번 입력하지 않게 하고 싶다.
- 사용자로서, 저장한 레시피 목록에서 필요 없는 것을 지우고 싶다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F3-1 | 회원가입 / 로그인 / 로그아웃 |
| F3-2 | 프로필 편집: 닉네임, 식단, 알레르기, 기본 인분 수 |
| F3-3 | 레시피 저장 (2단계 카드의 "저장" 연동) |
| F3-4 | 저장 목록 조회 (썸네일/제목/저장일) 및 상세 보기 |
| F3-5 | 저장 레시피 삭제 |
| F3-6 | 저장된 선호를 2단계 요청에 기본값으로 주입 |

## 5. 기술 설계
### 인증
- 세션 기반 또는 JWT 중 택1. 초기에는 이메일+비밀번호로 시작.
- 비밀번호는 해시(bcrypt/argon2) 저장, 평문 저장 금지.

### 데이터 모델 (예시)
```
User      { id, email, password_hash, nickname, created_at }
Profile   { user_id(FK), diet, allergies[], default_servings }
Recipe    { id, user_id(FK), title, ingredients(json), steps(json),
            minutes, difficulty, source_ingredients(json), created_at }
```

### 저장소
- 개발/학습용: SQLite (파일 기반, 설정 간단)
- 확장 시: PostgreSQL 등으로 마이그레이션

### 제안 API
```
POST /api/auth/signup        { email, password, nickname }
POST /api/auth/login         { email, password } -> token/session
GET  /api/profile            -> 프로필 + 선호
PUT  /api/profile            { diet, allergies, default_servings }
POST /api/recipes/save       { recipe }            -> 저장된 레시피
GET  /api/recipes/saved      -> 저장 목록
DELETE /api/recipes/saved/:id
```

## 6. UI/UX
- 상단 내비게이션: 로그인 상태 표시, "내 레시피", "프로필"
- 로그인 시 2단계 레시피 카드에 "저장" 버튼 활성화 (비로그인 시 로그인 유도)
- "내 레시피" 페이지: 저장 카드 그리드 + 상세 모달 + 삭제

## 7. 엣지 케이스 / 보안
- 중복 이메일 가입 방지
- 본인 레시피만 조회/삭제 가능 (권한 검사)
- 비로그인 상태에서 저장 시도 → 로그인/회원가입 유도
- 세션 만료 처리

## 8. 완료 기준 (Acceptance Criteria)
- [ ] 회원가입 후 로그인/로그아웃이 동작한다.
- [ ] 레시피를 저장하고 재방문 시 목록에서 볼 수 있다.
- [ ] 저장 레시피를 삭제할 수 있다.
- [ ] 저장된 선호가 2단계 기본값으로 채워진다.
- [ ] 다른 사용자의 레시피에 접근할 수 없다.

## 9. 의존성 / 참고
- 이전 단계: [PRD_step2.md](PRD_step2.md)
- 저장 대상 데이터는 2단계 레시피 구조와 호환되어야 함.
